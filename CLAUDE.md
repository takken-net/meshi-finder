# CLAUDE.md — このプロジェクトで作業するときの指示

**食べ探** — お気に入りの飲食店を「今いる場所」と「今食べたい料理」から探すスマホ向けアプリです。

Google マップに溜めたお気に入りは登録順に並ぶだけで、距離でもジャンルでも引き出せません。
それを、現在地からの距離・料理ジャンル・営業中かどうかで検索でき、
迷ったときはルーレットで1軒選んでくれる形にします。

このファイルは Claude Code が自動で読み込みます。作業前に必ず全体を確認してください。

---

## 最初にやること

```
tools/test.html をブラウザで開く
```

これだけでロジックの検証が走ります。**Node.js は使いません**（この環境に入っていないため）。
スマホのブラウザでも開けるので、実機で確かめたいときにも使えます。

---

## 技術構成

- **バニラJavaScript のみ。** フレームワーク・ビルドツール・npm パッケージは一切使いません
- ES Modules ではなく通常の `<script>` を順番に読み込む構成
- データはブラウザの localStorage（キー `meshi_finder_v1`）に保存。サーバー通信なし
- 画面はテンプレートリテラルで HTML 文字列を組み立て、`render()` で再描画する方式
- 公開先は **GitHub Pages**（無料・HTTPS）。リポジトリ名は `meshi-finder`

### ファイル構成

| ファイル | 責務 |
|---|---|
| `index.html` | 骨組みと**読み込み順の定義**（ここが唯一の依存順の記述） |
| `css/style.css` | 全画面の見た目。スマホ縦持ち前提・下部固定タブ |
| `js/01-utils.js` | 定数（`LS_KEY` `TABS`）、日付、**時刻**、距離、`normName`、`toast` |
| `js/02-genres.js` | ジャンル定義と Places のカテゴリ対応表 |
| `js/03-storage.js` | `DB` `load` `save` `migrate` `newShop` `putShop` `exportJSON` |
| `js/05-search.js` | **中核ロジック**。ジャンル判定、営業中判定、絞り込み、ルーレット |
| `js/06-router.js` | `render()` `renderResults()` `go()` と共通パーツ |
| `js/07-places.js` | Places API クライアント・キュー・日次カウンタ |
| `js/10-find.js` | 探す画面。現在地・ジャンル・キーワード |
| `js/11-result.js` | 結果画面（リスト／地図の切替） |
| `js/12-map.js` | 地図描画（Leaflet + OpenStreetMap） |
| `js/13-shop.js` | お店の一覧・登録・編集 |
| `js/14-import.js` | Takeout CSV 取込・重複判定 |
| `js/15-settings.js` | 設定 |
| `js/16-share.js` | 共有シートからの登録 |
| `js/20-boot.js` | `load(); handleShare(); render();` と Service Worker の登録 |
| `manifest.webmanifest` | PWA 定義と `share_target` |
| `sw.js` | オフライン用。**API と地図タイルはキャッシュしない** |
| `vendor/leaflet/` | 地図ライブラリ（CDN ではなく同梱） |
| `tools/test.html` | テストページ（これを開くと実行される） |
| `tools/tests.js` | テスト本体 |

**番号は読み込み順と必ず一致させています。**
Places 連携が `04` ではなく `07` なのは、`05-search.js` の
`normHours` / `guessGenres` を使うためです。

---

## 守ってほしいルール

### 1. 日付と時刻を混ぜない

**日付だけの値**（訪問日・登録日）は `'YYYY-MM-DD'` の文字列で持ち、
`today()` / `addDays()` / `diffDays()` / `D()` を使います。これらは**UTC基準**です。
`new Date('2026-08-07')` をローカル時刻で扱うと日本時間で1日ずれます。

**時刻**が要るのは営業中判定だけです。使ってよい関数は次の2つに限ります。

```js
nowWeekMin(d)         // 端末のローカル時刻 → 週の通し分
shopWeekMin(shop, d)  // 店の現地時刻 → 週の通し分
```

**ほかの場所で `getHours()` / `getDay()` を直接呼ばないでください。**
また、この手の関数は**必ず日時を引数で受け取ります**（中で `new Date()` を暗黙に呼ばない）。
テストで時刻を差し込めなくなるためです。

### 2. `index.html` の script タグの順番を変えない

前のファイルで定義した `const` を後のファイルが使います。順番を変えると起動しません。
ファイルを追加するときは、**使う側より前**に読み込むよう1行足してください。
`tools/test.html` にも同じ行を足してください（`20-boot.js` だけは読み込みません）。

### 3. `render()` と `renderResults()` を使い分ける

| 関数 | 使うとき |
|---|---|
| `render()` | タブを切り替えたとき。画面全体を組み直す |
| `renderResults()` | 絞り込み条件が変わったとき。結果の箱だけ差し替える |

**キーワード入力のたびに `render()` を呼ぶと、日本語変換の途中で入力欄が作り直されて
確定できなくなります。** 入力中は必ず後者を使ってください。
同じ理由で、チップの選択なども「変わった部分だけ差し替える」形にしています。

### 4. データを変更したら `save(); render();`

この2つをセットで呼ぶのが全編の作法です。片方だけだと保存漏れ・表示崩れになります。

### 5. ユーザー入力を埋め込むときは必ず `esc()`

`${esc(s.memo)}` のように。数値は `num()` で正規化し、表示は `fmt()` / `fmt1()` を使います。

### 6. 地図は `render()` の冒頭で破棄し、`AFTER[TAB]` で生成する

Leaflet のインスタンスを残したまま `innerHTML` を書き換えると、
イベントが宙に浮いて後で落ちます。この対で必ず扱ってください。

### 7. データ構造を変えたら `migrate()` に移行処理を書く

ユーザーの手元には既存データが localStorage に入っています。
`newShop()` にフィールドを足したら `migrate()` にも既定値の穴埋めを追記してください。

### 8. 変更したらテストを通す

`tools/test.html` を開く。計算式や判定ロジックを触ったときは `tools/tests.js` に**検証も追加**してください。

---

## APIキーのリファラ制限は「ドメインまで」で書く（実機で確認済み）

ブラウザの既定の Referrer-Policy（strict-origin-when-cross-origin）により、
Google へ送られる Referer は **`https://takken-net.github.io/` のようにドメイン部分だけ**になる。
そのため Google Cloud 側の「アプリケーションの制限」に
`https://takken-net.github.io/meshi-finder/*` と書くと**一致せず全リクエストが403になる**。

正しくは:
```
https://takken-net.github.io/*
```
実際にこの403を踏んで直した経緯があるので、パス付きに「直さない」こと。

## Places API を扱うときの決まり

**課金事故を防ぐための取り決めです。勝手に緩めないでください。**

1. **API を叩くのは `js/04-places.js` の1関数だけ。** 呼び出し箇所を増やさない
2. **フィールドマスクを増やすときは SKU 帯が上がらないか必ず確認する。**
   営業時間・評価は Enterprise 帯（無料枠が月1,000回程度）、
   座標・ジャンルまでは Pro 帯（月5,000回）。`settings.fetchHours` で切り替えられるようにしてある
3. **APIキーはリポジトリに絶対にコミットしない。** `DB.settings.apiKey` にのみ存在する
   （HTTPリファラ制限はブラウザ経由しか防げず、`curl` では偽装できるため、キーを外に出さないことが本質）
4. **自動リトライループを作らない。** 失敗はデータに記録し、人間が再開ボタンを押す
5. **1店につき1回だけ叩く。** `placeId` が入っている店は二度と叩かない
6. `openNow` は使わない。取得時点のスナップショットなので、保存すると永久に固定される

---

## 業務ロジックの要点（勝手に変えないでください）

### 店のデータは「Places 由来」と「ユーザー資産」に分かれている

`myRate` `tags` `memo` `fav` `visits` `genresManual` は**ユーザー資産**です。
Places の再取得でも CSV の再取込でも**絶対に上書きしません**。

`types`（Places のカテゴリ）は英語のまま生で保存します。
ジャンル対応表を後から改良したとき、**API を叩き直さずに再分類できる**ようにするためです。

### ジャンルは3段で判定する（`guessGenres`）

1. `primaryType`（Places が代表と判断したカテゴリ）を最優先
2. 決まらなければ `types` 全体を見る
3. **店名・種別の和名・メモのキーワードは「常に追加で」走らせる**
   （Places が `japanese_restaurant` としか返さない店でも、店名に「麺屋」があればラーメンが付く）

1店が複数ジャンルを持つのは許容します（居酒屋かつ焼肉など実態に合うため）。
手で編集した店は `genresManual` が立ち、以降の自動判定で上書きしません。

### 営業時間は「週の通し分の範囲リスト」に正規化して保存する（`normHours`）

Places の `periods` をそのまま持たず、`{s, e}` の配列に直します。
日曜0:00 を 0、土曜23:59 を 10079 とする通し分です。

- 中休みは範囲が複数になるだけ
- 日またぎは `e` が翌日の値になる（月18:00〜火02:00）
- **週またぎ（土20:00〜日03:00）は範囲を2本に割る**。ここが一番間違えやすい
- 24時間営業は `close` が無い → `always: true`
- 定休日はその曜日の範囲が無いだけ（明示されない）

判定は `m >= s && m < e`。**開店時刻ちょうどは営業中、閉店時刻ちょうどは営業時間外**です。

### 営業時間が「不明」な店を機械的に消さない

`isOpen()` は `true` / `false` / `null`（未取得）の3値を返します。
`null` を除外すると Places 未解決の店が全部消えて画面が空に見えるため、
`settings.unknownHours`（既定 ON）で不明も表示します。

⚠️ `regularOpeningHours` は**祝日・臨時休業を反映しません**。さらに保存するので古くなります。
営業中バッジには注記を添え、Google マップへのリンクを併置して最終確認を促してください。

### 距離で絞るときは位置不明の店を外す

「500m以内」に位置の分からない店を混ぜると紛らわしいためです。
外した件数は画面に「位置未取得の N 軒は除外」と出します。

### Takeout はお店の記録が2つのサービスに分かれている（実物で確認済み）

| Takeout のサービス名 | 出てくるファイル | 中身 |
|---|---|---|
| **保存済み** | リストごとの **CSV**（お気に入りの場所.csv など） | お気に入り・行きたい場所・自作リスト。**座標なし** |
| **マップ（マイプレイス）** | **保存した場所.json** ＋ クチコミ.json | スター付きの場所。**GeoJSON で座標入り** |

当初「マップ（自分のプレイス）に CSV が入っている」と想定していたが、**実物は JSON だった**。
取込は両形式に対応済み（`readImportFile` が拡張子と中身で自動判別）。

⚠️ `保存した場所.json` の座標は GeoJSON なので **[経度, 緯度] の順**。逆に読むと全世界にばらまかれる。
`[0,0]` は「座標なし」の意味なので null に落とす（`parseSavedJson`）。

### 重複判定は4段。名前だけで勝手にまとめない（`findDup`）

| 優先 | キー | 判定 |
|---|---|---|
| 1 | `placeId` 一致 | 既存 |
| 2 | `srcId` 一致（cid / ftid） | 既存 |
| 3 | **`srcUrl` 一致** | 既存 |
| 4 | 名前一致 かつ 両方に座標 かつ **200m以内** | 既存 |
| — | 名前一致 だが 座標不明 or 離れている | **要確認**（既定は別の店として追加） |

⚠️ 「サイゼリヤ」「日高屋」のように**同名の店が何十軒もあります**。
名前が一致しただけでまとめると別の店を潰すため、座標で裏を取れないものは人に判断させます。

⚠️ **3番目の URL 一致は外さないでください。** 短縮URL（`maps.app.goo.gl`）は
ブラウザから展開できず、座標も place_id も取れません。URL 一致が無いと
**同じ CSV を取り込み直すたびに同名の店が増えます**（実際にテストで発覚しました）。

### 取り込み元のリストは記録し、`mergeShop` では「足し算」する

`shop.lists` に「行きたい場所」「仕事用 / お気に入り」などの取り込み元を持ちます。
**複数の Google アカウントのお気に入りを1つに合流できる**ようにするためのものです。

ほかのユーザー資産は再取込で一切触りませんが、**`lists` だけは例外で追記します**。
同じ店が複数のリスト・複数のアカウントに入っているのが普通だからです（重複はしません）。

### キーワードは空白区切りの AND 検索

店名・種別・住所・メモ・タグ・ジャンル名を横断します。
`normName()` を検索・重複判定・ジャンル判定で**共通に**使うので、
「ラーメン」「らーめん」「ﾗｰﾒﾝ」が同じものとして扱われます。

OR 検索やスコアリングは作りません（数百件規模では過剰なため）。

### その他の決定事項

- 検索用キャッシュ `_hay` は**保存に含めない**（`save()` が除外している）
- ルーレットは `RAND` 経由で乱数を引く（テストで固定値に差し替えるため）。直前と同じ店は避ける
- 訪問記録は残すが、**提案の優先度には使わない**（今回は採用しない方針）
- 原材料費のような金額表示は無い。このアプリは検索専用

---

## データ構造

```js
DB = {
  shops: [{
    id, name, nameKey,
    // Places 由来（一度取得したら再取得しない）
    placeId, lat, lng, addr, types:[], primaryType, typeJa,
    hours,              // {always, ranges:[{s,e}], text:[]} / 未取得は null
    utcOffset, bizStatus, gRating, gCount, priceLevel, mapsUri, fetchedAt,
    // ユーザー資産（絶対に上書きしない）
    genres:[], genresManual, tags:[], myRate, memo, fav,
    lists:[],           // 取り込み元のリスト名／アカウント。ここだけは「足し算」で増やす
    // 管理
    srcUrl, srcId, src, status, err, cands, createdAt, updatedAt
  }],
  visits:   [{ id, shop, date, memo }],
  genres:   [{ id, label, icon, types:[], words:[] }],
  queue:    ['S-xxxxxx'],                     // Places 未解決の処理待ち
  settings: { apiKey, fetchHours, dailyLimit, usage:{date,n},
              places:[{id,label,lat,lng}], lastPos, unknownHours, lastRoulette },
  _v: 1
}
```

---

## 進み具合

- [x] **Phase 0** 骨組み・保存・お店の登録・テスト基盤
- [x] **Phase 1** 検索の中核（距離・ジャンル・キーワード・ルーレット・営業中判定）
- [x] **Phase 2** Takeout CSV 取込（パーサ・URL抽出・重複判定）
- [x] **Phase 3** Places API 連携（キー設定・キュー・営業時間の取得）
- [x] **Phase 4** 地図タブ（Leaflet + OpenStreetMap）
- [x] **Phase 5** PWA と共有シートからの登録
- [ ] **Phase 6** 磨き込み（GitHub Pages への公開後、実機で触ってから）

**まだ実機で試していない箇所**（公開しないと確かめられないもの）
- 現在地の取得（HTTPS が要る）
- ホーム画面への追加と、共有シートに出るか
- Google マップの共有が実際にどんな `title` / `text` を送ってくるか
  → `parseShared` は推測で書いてあるので、実物を見て調整すること

---

## 未対応・今後の候補

- 地図は Leaflet を `vendor/` に同梱する予定（CDN にするとオフラインで動かなくなる）
- iPhone は共有シートへの登録に非対応。ショートカットアプリ経由なら可能（今回は Android 前提）
- 複数端末でのデータ同期は未対応（localStorage は端末ごとに独立）
- 1つの店を複数人で共有する使い方は未対応
